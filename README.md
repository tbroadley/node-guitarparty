# guitarparty

```js
var Guitarparty = require('guitarparty');

var api = new Guitarparty('YOUR_API_KEY');

api.request('endpoint', (err, data) => {
  if (err) {
    // handle error
  }
  // handle data
});
```
